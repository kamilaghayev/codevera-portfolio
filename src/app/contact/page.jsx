import { ContactPage } from '@/app-pages';
import { metadataContact } from '@/config/seo.config';

export const metadata = metadataContact;

export default function Contact() {
	return (
		<ContactPage/>
	)
}