import { PortfolioPage } from "@/app-pages"
import { metadataPortfolio } from '@/config/seo.config'

export const metadata = metadataPortfolio;

export default function Portfolio() {
    return (
        <PortfolioPage/>
    )
}